package de.tu.darmstadt.tk.bonus.m1.group.project.facebook;


/*import com.restfb.DefaultFacebookClient;
import com.restfb.FacebookClient;
import com.restfb.types.Page;
import com.restfb.types.User;*/

public class FacebookIntegration {
	
	
	public static void main (String[] args) {
		
		/*FacebookClient facebookClient = new DefaultFacebookClient(PrivateTokens.BALU_LONG_TERM_TOKEN);
		

		// For all API calls, you need to tell RestFB how to turn the JSON
		// returned by Facebook into Java objects.  In this case, the data
		// we get back should be mapped to the User and Page types, respectively.
		// You can write your own types too!

		User user = facebookClient.fetchObject("me", User.class);
		Page page = facebookClient.fetchObject("m1ambient", Page.class);
		System.out.println("User name: " + user.getName());
		System.out.println("Page likes: " + page.getPlotOutline());	*/
	}
}
